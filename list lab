
#Q1: Write a function that takes a list as a parameter, and then returns the sum of all the items in that list.
list1 = [2, 3, 4, 5, 15, 1, 43, 20]
def myFunction (list1):
    x = sum(list1)
    return x
print (myFunction(list1))

### Q2: Write a function that takes a list as a parameter, and then returns the largest number from a list of numbers.
def theMaxOfList (list1):
    max_list1 = max ( list1)
    return max_list1
print (theMaxOfList(list1))


#Q3: Create an odd numbers list from the elements of a range from 1200 to 2000 with steps of 125, using [ list comprehension ].

primes = [num for num in range(1200,2000,125) if num % 2!= 0]
print (primes)


#Q4: use list slicing to get a new list from the previous list starting from the start to the 5th element in the list.
newListOfPrimes= list1  [:5]
print (newListOfPrimes)